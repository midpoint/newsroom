<div class="list-group">
  <% stories.each do |story| %>
    <%= link_to story.url,
      class: "list-group-item list-group-item-action" do %>
      <%= story.feed.title %> &bullet; <%= story.title %>
    <% end %>
  <% end %>
</ul>
